---
title: "Quick Start"
description: "Install Zen and run your first security scan"
---

## Prerequisites

- Docker (running)
- An LLM API key from any [supported provider](/llm-providers/overview) (OpenAI, Anthropic, Google, etc.)

## Installation

<Tabs>
  <Tab title="curl">
    ```bash
    curl -sSL https://zenney.uk/install | bash
    ```
  </Tab>
  <Tab title="pipx">
    ```bash
    pipx install zen-agent
    ```
  </Tab>
</Tabs>

## Configuration

Set your LLM provider:

```bash
export ZEN_LLM="openai/gpt-5.4"
export LLM_API_KEY="your-api-key"
```

<Tip>
For best results, use `openai/gpt-5.4`, `anthropic/claude-opus-4-6`, or `openai/gpt-5.2`.
</Tip>

## Run Your First Scan

```bash
zen --target ./your-app
```

<Note>
First run pulls the Docker sandbox image automatically. Results are saved to `zen_runs/<run-name>`.
</Note>

## Target Types

Zen accepts multiple target types:

```bash
# Local codebase
zen --target ./app-directory

# GitHub repository
zen --target https://github.com/org/repo

# Live web application
zen --target https://your-app.com

# Multiple targets (white-box testing)
zen -t https://github.com/org/repo -t https://your-app.com

# Targets from a file, one target per non-empty, non-comment line
zen --target-list ./targets.txt
```

## Next Steps

<CardGroup cols={2}>
  <Card title="CLI Options" icon="terminal" href="/usage/cli">
    Explore all command-line options.
  </Card>
  <Card title="Scan Modes" icon="gauge" href="/usage/scan-modes">
    Choose the right scan depth.
  </Card>
</CardGroup>
