name: Build & Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            target: macos-arm64
            wheel-platform: macosx_11_0_arm64
          - os: macos-15-intel
            target: macos-x86_64
            wheel-platform: macosx_11_0_x86_64
          - os: ubuntu-22.04
            target: linux-x86_64
            wheel-platform: manylinux_2_17_x86_64
          - os: ubuntu-22.04-arm
            target: linux-arm64
            wheel-platform: manylinux_2_17_aarch64
          - os: windows-latest
            target: windows-x86_64
            wheel-platform: win_amd64

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4.4.0
        with:
          persist-credentials: false

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
        with:
          python-version: '3.12'

      - uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86  # v5.4.2

      - uses: actions/setup-go@44694675825211faa026b3c33043df3e48a5fa00  # v6.0.0
        with:
          go-version: '1.24.x'
          check-latest: true
          cache-dependency-path: zen/interface/tui/go.sum

      - name: Build
        shell: bash
        env:
          ZEN_WHEEL_PLATFORM_TAG: ${{ matrix.wheel-platform }}
        run: |
          uv sync --frozen
          uv build --wheel
          uv run python -c 'import glob, os, sys, zipfile; wheels = glob.glob("dist/*.whl"); assert len(wheels) == 1, wheels; archive = zipfile.ZipFile(wheels[0]); tui = "zen/bin/zen-tui.exe" if sys.platform == "win32" else "zen/bin/zen-tui"; assert tui in archive.namelist(); metadata = archive.read(next(name for name in archive.namelist() if name.endswith(".dist-info/WHEEL"))).decode(); assert "Root-Is-Purelib: false" in metadata; assert "Tag: py3-none-" + os.environ["ZEN_WHEEL_PLATFORM_TAG"] in metadata'

          uv run pyinstaller zen.spec --noconfirm

          if [[ "${{ runner.os }}" == "Windows" ]]; then
            PYI_BINARY="dist/zen.exe"
            TUI_NAME="zen-tui.exe"
            dist/zen.exe --version
          else
            PYI_BINARY="dist/zen"
            TUI_NAME="zen-tui"
            dist/zen --version
          fi
          uv run pyi-archive_viewer -l "$PYI_BINARY" | grep -E "zen[/\\]+bin[/\\]+$TUI_NAME" >/dev/null

          if [[ "${{ matrix.target }}" == "linux-arm64" ]]; then
            file dist/zen
            file dist/zen | grep -q "ARM aarch64" || {
              echo "::error::linux-arm64 artifact is not an ARM aarch64 binary"
              exit 1
            }
          fi

          VERSION=$(grep '^version' pyproject.toml | head -1 | sed 's/.*"\(.*\)"/\1/')
          mkdir -p dist/release

          if [[ "${{ runner.os }}" == "Windows" ]]; then
            cp dist/zen.exe "dist/release/zen-${VERSION}-${{ matrix.target }}.exe"
            (cd dist/release && 7z a "zen-${VERSION}-${{ matrix.target }}.zip" "zen-${VERSION}-${{ matrix.target }}.exe")
          else
            cp dist/zen "dist/release/zen-${VERSION}-${{ matrix.target }}"
            chmod +x "dist/release/zen-${VERSION}-${{ matrix.target }}"
            tar -C dist/release -czvf "dist/release/zen-${VERSION}-${{ matrix.target }}.tar.gz" "zen-${VERSION}-${{ matrix.target }}"
          fi

      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4.6.2
        with:
          name: zen-${{ matrix.target }}
          path: |
            dist/release/*.tar.gz
            dist/release/*.zip
            dist/*.whl
          if-no-files-found: error

  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093  # v4.3.0
        with:
          path: release
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65  # v2.6.2
        with:
          prerelease: ${{ !startsWith(github.ref, 'refs/tags/') }}
          generate_release_notes: true
          files: release/**

  publish-pypi:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093
        with:
          path: dist
          merge-multiple: true

      - name: Collect wheels
        run: |
          mkdir -p publish
          find dist -name "*.whl" -exec cp {} publish/ \;
          ls -la publish/
          echo "Found $(ls publish/*.whl | wc -l) wheel(s)"

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: publish/
